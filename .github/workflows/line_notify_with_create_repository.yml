name: Push Commit Notification

on:
  push:
    branches:
      - main
      - develop

jobs:
  notify_on_commit:
    runs-on: ubuntu-latest
    steps:
      - name: Install jq
        run: sudo apt-get install jq
        
      - name: Call API on push
        env:
          COMMIT_URL: ${{ github.event.head_commit.url }}
          COMMIT_AUTHOR: ${{ github.event.head_commit.author.name }}
          COMMIT_SHA: ${{ github.event.after }}
          COMMIT_MESSAGE: ${{ github.event.head_commit.message }}
          COMMIT_DATE: ${{ github.event.head_commit.timestamp }}
          BRANCH_NAME: ${{ github.ref_name }}
          GITHUB_EVENT: ${{ toJSON(github.event) }}
          ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
        run: |
          # 変更されたファイルの一覧を取得
          # `commits`配列がnullではないかを確認してからjqを実行
          if [[ "${{ github.event.commits }}" == "" ]]; then
            file_list="変更なし"
          else
            file_list=$(echo "$GITHUB_EVENT" | jq '[.commits[] | .added, .modified, .removed] | flatten | unique | join("\n")')
            # jqが出力する二重引用符を削除
            file_list=$(echo "$file_list" | sed 's/^"//; s/"$//')
          fi
          
          # Flex MessageのJSONペイロードを作成
          payload=$(jq -n \
            --arg message "$COMMIT_MESSAGE" \
            --arg author "$COMMIT_AUTHOR" \
            --arg url "$COMMIT_URL" \
            --arg sha "$COMMIT_SHA" \
            --arg date "$COMMIT_DATE" \
            --arg branch "$BRANCH_NAME" \
            --arg file_list "$file_list" '{
            "to": "${{ secrets.GROUP_ID }}",
            "messages": [
              {
                "type": "flex",
                "altText": "新しいコミットがプッシュされました。",
                "contents": {
                  "type": "bubble",
                  "header": {
                    "type": "box",
                    "layout": "horizontal",
                    "contents": [
                      {
                        "type": "box",
                        "layout": "vertical",
                        "contents": [],
                        "cornerRadius": "5000px",
                        "width": "16px",
                        "height": "16px",
                        "spacing": "none",
                        "margin": "none",
                        "backgroundColor": "#57ab5b",
                        "offsetTop": "2px"
                      },
                      {
                        "type": "box",
                        "layout": "vertical",
                        "contents": [
                          {
                            "type": "text",
                            "text": $message,
                            "color": "#d0d7e0",
                            "wrap": true,
                            "action": {
                                "type": "uri",
                                "label": "action",
                                "uri": $url
                            },
                            "decoration": "underline"
                          },
                          {
                            "type": "text",
                            "wrap": true,
                            "text": ($sha | cut -c1-7),
                            "color": "#666666"
                          }
                        ]
                      }
                    ],
                    "backgroundColor": "#141c21",
                    "alignItems": "flex-start",
                    "spacing": "md"
                  },
                  "body": {
                    "type": "box",
                    "layout": "vertical",
                    "contents": [
                      {
                        "type": "box",
                        "layout": "vertical",
                        "margin": "md",
                        "spacing": "none",
                        "contents": [
                          {
                            "type": "text",
                            "text": "📅 日時: " + ($date | fromdateiso8601 | strftime("%Y-%m-%d %H:%M:%S")),
                            "wrap": true,
                            "color": "#666666",
                            "size": "sm"
                          },
                          {
                            "type": "text",
                            "text": "🌳 ブランチ: " + $branch,
                            "wrap": true,
                            "color": "#666666",
                            "size": "sm"
                          },
                          {
                            "type": "separator"
                          },
                          {
                            "type": "text",
                            "text": "✏️ 変更されたファイル:",
                            "weight": "bold",
                            "color": "#666666",
                            "size": "sm",
                            "margin": "md"
                          },
                          {
                            "type": "text",
                            "text": $file_list,
                            "wrap": true,
                            "color": "#666666",
                            "size": "sm"
                          }
                        ]
                      }
                    ]
                  },
                  "footer": {
                    "type": "box",
                    "layout": "vertical",
                    "contents": [
                      {
                        "type": "text",
                        "text": "🧑‍💻 作成者: " + $author,
                        "size": "xs",
                        "color": "#666666"
                      }
                    ],
                    "alignItems": "flex-end",
                    "backgroundColor": "#F3F3F3"
                  }
                }
              }
            ]
          }')" >> $GITHUB_OUTPUT

      - name: Call API on push
        env:
          ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
        run: |
          curl -v -X POST https://api.line.me/v2/bot/message/push \
          -H 'Content-Type: application/json' \
          -H "Authorization: Bearer $ACCESS_TOKEN" \
          -d "${{ steps.prepare_payload.outputs.payload }}"