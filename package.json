{
  "name": "shinshu-fesnav-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend:dev": "cd backend && pipenv run flask run --host=0.0.0.0 --port=5051 --debug",
    "backend:prod": "cd backend && export FLASK_ENV=production && pipenv run gunicorn --workers 4 --bind 0.0.0.0:5051 \"app:create_app()\"",
    "frontend": "cd frontend && npm start",
    "build-frontend": "cd frontend && npm run build",
    "dev": "concurrently \"npm:backend:dev\" \"npm:frontend\"",
    "prod": "npm run build-frontend && npm run backend:prod"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@fullcalendar/list": "^6.1.19",
    "@fullcalendar/timegrid": "^6.1.19",
    "leaflet": "^1.9.4",
    "react-leaflet": "^5.0.0"
  }
}
