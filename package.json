{
  "name": "shinshu-fesnav-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start-backend": "cd backend && pipenv run flask run --host=0.0.0.0 --port=5051 --debug",
    "start-react": "cd frontend && npm start",
    "dev": "concurrently \"npm:start-backend\" \"npm:start-react\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
